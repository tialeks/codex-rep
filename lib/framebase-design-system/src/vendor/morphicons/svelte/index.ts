/* Source-side entry for `morphicons/svelte`. The published entry
   (dist/svelte.js + dist/svelte.d.ts) is generated by scripts/build-svelte.ts;
   this file exists so tests and the typechecker consume the same typed
   surface: svelte's own ambient `*.svelte` module types the import as the
   loose LegacyComponentType, pinned here to the precise
   Component<Props, Exports> signature. */

import type { Component } from "svelte";
import MorphIconComponent from "./MorphIcon.svelte";
import type { MorphHandle, MorphIconProps } from "./shared";

export const MorphIcon = MorphIconComponent as unknown as Component<
  MorphIconProps,
  MorphHandle
>;

export type { SpringPreset } from "../core/spring";
export type { IconInput, IconNode, Sampled } from "../core/types";
export type { Morph, MorphOptions, PathEl, ReducedMotionMode } from "../dom/index";
export type { MorphHandle, MorphIconProps };
